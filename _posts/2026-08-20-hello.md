---
title: "Hello"
---

This is the first post on the new, deliberately plain version of this site. No theme to fight, no build pipeline, just markdown files and one stylesheet.

To publish a new post: add a file to `_posts/` named `YYYY-MM-DD-slug.md` with a `title` in the frontmatter, then `git push`. GitHub Pages rebuilds the site automatically.
