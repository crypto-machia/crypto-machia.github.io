source "https://rubygems.org"

# This mirrors exactly what GitHub Pages runs, so what you see locally
# is what you get live. No need to manage the Jekyll version yourself.
gem "github-pages", group: :jekyll_plugins

# Needed for local preview on newer Ruby versions
gem "webrick"
